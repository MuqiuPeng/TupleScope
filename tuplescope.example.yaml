# TupleScope workspace — copy to tuplescope.yaml and point it at your backend.
#
#   cp tuplescope.example.yaml tuplescope.yaml
#   pnpm start
#
# The runtime prints a URL carrying a per-session access token. Open that.

name: My API
baseUrl: http://127.0.0.1:8000

database:
  # A development database. TupleScope holds a real connection to it and reads
  # what your API writes, so never point this at anything you care about.
  #
  # The password is a `${secret:…}` reference, not a value. This file is meant
  # to be committed, and a template that spells a password inline teaches the
  # one pattern the rest of this document argues against. Set it once with
  # `tuplescope secret set db_password`; it goes to your OS keychain and never
  # to disk here. `${PGPASSWORD}` or a plain `${VAR}` works too — anything but
  # a literal.
  connectionString: postgresql://user:${secret:db_password}@127.0.0.1:5432/myapp_dev

# Where your scenario files live, relative to this file.
scenariosDir: scenarios

# Optional. Called before any dataset that declares `resetFirst: true`.
# resetUrl: http://127.0.0.1:8000/test/reset

# Who a step can act as. Multi-party flows — transfers, approvals, anything
# with two sides — cannot be expressed with a single ambient token.
identities:
  - id: alice
    header: { name: authorization, value: "Bearer <token>" }
  - id: bob
    header: { name: authorization, value: "Bearer <token>" }

# Hidden from the diff, never from `hasWrite`. An idempotency assertion still
# sees a retry that only touched a timestamp.
ignoreColumns:
  - updated_at
  - last_seen

# Redacted at capture, before the value is stored or serialised anywhere.
maskColumns:
  - password_hash
  - card_number
  - api_token

# Watch an idle window before each run. Anything that writes during it is not
# your scenario — background jobs, session sweepers, outbox pollers — and the
# report says so instead of blaming your API. Set to 0 to skip.
baselineWindowMs: 400

# Stored runs live in .tuplescope/runs next to this file, newest 50 kept.
# `tuplescope runs` lists them; `--from` and `--only` read a previous run's
# variables from there, which is the only reason the store exists.
# Disable with --no-save.

# Charts drawn beside the diff, from what the run observed.
#
# Names and expressions only — the host evaluates and draws them, in its own
# palette. A step that did not touch the row is drawn as inference rather than
# as a value, because the run knows the difference. Sources must select a
# column: `after(updated(t, k = "x").col)`, not `single(...)`, which throws on a
# step that touched nothing.
#
# panels:
#   - title: Wallets
#     unit: USD
#     sources:
#       alice: after(updated(wallets, id = "wal_alice").balance)
#       shop: after(updated(wallets, id = "wal_shop").balance)
